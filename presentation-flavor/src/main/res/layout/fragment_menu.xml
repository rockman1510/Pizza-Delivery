<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="20dp">

        <Button
            android:id="@+id/btOrder"
            style="@style/TextHeader"
            android:enabled="@{isEnableOrder}"
            android:paddingStart="15dp"
            android:paddingEnd="15dp"
            android:text="@string/order"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <TextView
            android:id="@+id/tvTotal"
            style="@style/TextHeader"
            android:layout_marginEnd="20dp"
            android:gravity="center"
            android:text="@{@string/total_price(totalPrice)}"
            android:textSize="20sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/btOrder"
            app:layout_constraintTop_toTopOf="@id/btOrder" />

        <TextView
            android:id="@+id/tvTitle"
            style="@style/TextTitle"
            android:text="@string/menu"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvFlavor"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="20dp"
            android:adapter="@{adapter}"
            app:layout_constraintBottom_toTopOf="@id/btOrder"
            app:layout_constraintTop_toBottomOf="@id/tvTitle" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <data>

        <variable
            name="adapter"
            type="com.huylv.presentation_flavor.ui.MenuAdapter" />

        <variable
            name="isEnableOrder"
            type="boolean" />

        <variable
            name="totalPrice"
            type="float" />
    </data>
</layout>