<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="20dp">

        <TextView
            android:id="@+id/tvBill"
            style="@style/TextHeader"
            android:layout_marginTop="60dp"
            android:text="@string/your_bill"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvFlavor1"
            style="@style/TextSubHeader"
            android:layout_marginTop="30dp"
            android:text="@{pizzaModel.flavors.size() == 2 ? pizzaModel.flavors.get(0).name : ``}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvBill" />

        <TextView
            android:id="@+id/tvFlavor2"
            style="@style/TextSubHeader"
            android:layout_marginTop="20dp"
            android:text="@{pizzaModel.flavors.size() == 2 ? pizzaModel.flavors.get(1).name : ``}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvFlavor1" />

        <TextView
            android:id="@+id/tvTotal"
            style="@style/TextSubHeader"
            android:layout_marginTop="20dp"
            android:text="@{@string/total_price(pizzaModel.price)}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvFlavor2" />


        <Button
            android:id="@+id/btClose"
            style="@style/TextHeader"
            android:layout_marginTop="30dp"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:text="@string/close"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvTotal" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <data>

        <variable
            name="pizzaModel"
            type="com.huylv.presentation_flavor.model.PizzaModel" />
    </data>
</layout>